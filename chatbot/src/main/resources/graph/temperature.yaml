name: temperature
nodeType: step
type: text

message: "Please type and send your current body temperature:\n(For example, if your current body temperature is 99°F,
 type and send 99 as a response)"

validationRequired: true

customValidator:
  class: org.egov.chat.xternal.validator.RangeValidator
  params:
    min: 96
    max: 106
    rawInput: /message/rawInput

errorMessage: Sorry, I did not understand. Valid body temperature range is from 96.0 to 106.0.
